# Code Map fixture v1

Task 2A reserves this fixture for the staged scheduler Case. Task 4 freezes the actual Git commits, file bytes, exclusions, and SHA-256 manifest. The fixture is local-only and must not use the developer worktree as its Git cache.
